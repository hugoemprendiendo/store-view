rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
        return request.auth.uid == userId;
    }

    function isSuperAdmin() {
      // This relies on the user having read access to their own document.
      return isSignedIn() && exists(/databases/$(database)/documents/users/$(request.auth.uid)) 
          && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'superadmin';
    }
    
    function isAssignedToBranch(branchId) {
        let userDocData = get(/databases/$(database)/documents/users/$(request.auth.uid)).data;
        return isSuperAdmin() || (
            'assignedBranches' in userDocData &&
            branchId in userDocData.assignedBranches
        );
    }
    
    match /users/{userId} {
      allow create: if isOwner(userId);
      // A user must be able to read their own document for isSuperAdmin() to work.
      // A superadmin can get any user's profile.
      allow get: if isOwner(userId) || isSuperAdmin();
      // Listing and updating is restricted to superadmins.
      allow list, update: if isSuperAdmin();
      allow delete: if false; // Nobody can delete user profiles
    }

    match /branches/{branchId} {
      allow get: if isAssignedToBranch(branchId);
      allow list: if isSuperAdmin();
      allow create, update, delete: if isSuperAdmin();
    }

    match /incidents/{incidentId} {
      function getIncidentBranchId() {
        return request.method == 'create' ? request.resource.data.branchId : resource.data.branchId;
      }
      
      allow get, write: if isAssignedToBranch(getIncidentBranchId());
      
      // Superadmins can list all incidents.
      // Regular users can list incidents ONLY if they filter by a specific branchId, and they must be assigned to it.
      allow list: if isSuperAdmin() || (
        isSignedIn() &&
        'branchId' in request.query.where &&
        isAssignedToBranch(request.query.where.branchId)
      );
    }
    
    match /app_settings/{settingId} {
        allow get: if isSignedIn();
        allow write: if isSuperAdmin();
    }

    // Rule for the test collection
    match /test_docs/{docId} {
        allow read, write: if true;
    }
  }
}
